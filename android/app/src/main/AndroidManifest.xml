<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".PiyushApp"
        android:label="PiyushOS v1.5"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.PiyushOS"
        android:usesCleartextTraffic="true"
        android:allowBackup="true">

        <!-- SABSE PEHLI app code: crash handler + boot canary (Application se bhi pehle) -->
        <provider
            android:name=".CrashGuardProvider"
            android:authorities="com.piyushos.app.crashguard"
            android:exported="false"
            android:initOrder="1" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ShareActivity" android:exported="false" />

        <!-- Phone control ka engine (tap/swipe/type/app kholna) -->
        <service
            android:name=".accessibility.PhoneControllerService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Screenshot ke liye (MediaProjection) -->
        <service
            android:name=".media.ProjectionService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- ML Kit ki Firebase eager-init provider HATA di (app launch pe crash suspect).
             ML Kit ab LAZY init hoga - pehle OCR use hone par. -->
        <provider
            android:name="com.google.mlkit.common.internal.MlKitInitProvider"
            tools:node="remove" />

        <!-- AndroidX ka auto-startup provider bhi HATA di (emoji2/lifecycle/profileinstaller).
             Ab app khulte waqt sirf PIYUSHOS ki apni code chalti hai - koi bhi third-party
             auto-init nahi. Isse "startup crash" ki possibility sabse kam ho jaati hai. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            tools:node="remove" />

    </application>

</manifest>
